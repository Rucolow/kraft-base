# apps/staff — environment variables (copy to apps/staff/.env)

# Supabase (Phase 1+)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# PowerSync (Phase 1+)
VITE_POWERSYNC_URL=
VITE_POWERSYNC_TOKEN=

# Web Push / VAPID (Phase 6+)
VITE_VAPID_PUBLIC_KEY=
